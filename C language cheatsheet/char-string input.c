char znak, rijec[20], recenica[20];
scanf(" %c", &znak); // unesi znak; razmak je na početku da prekočimo prethodni ENTER po potrebi
scanf("%s", rijec); // unesi riječ;
scanf("%[^\n]", recenica); // unesi rečenicu; čita sa konzole dok se ne pritisne ENTER
